CREATE TABLE ENTRADA (
    ID_ENTRADA NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_EQUIPAMENTO NUMBER NOT NULL,
    ID_USUARIO NUMBER NOT NULL,
    ID_RESPONSAVEL NUMBER NOT NULL,
    DT_ENTRADA DATE NOT NULL,
    OBSERVACAO VARCHAR2(400)
);

ALTER TABLE ENTRADA ADD CONSTRAINT PK_ENTRADA_ID PRIMARY KEY (ID_ENTRADA);
ALTER TABLE ENTRADA ADD CONSTRAINT FK_ENTRADA_EQUIPAMENTO FOREIGN KEY (ID_EQUIPAMENTO) REFERENCES EQUIPAMENTO(ID_EQUIPAMENTO);
ALTER TABLE ENTRADA ADD CONSTRAINT FK_ENTRADA_USUARIO FOREIGN KEY (ID_USUARIO) REFERENCES USUARIO(ID_USUARIO);
ALTER TABLE ENTRADA ADD CONSTRAINT FK_ENTRADA_RESPONSAVEL FOREIGN KEY (ID_RESPONSAVEL) REFERENCES RESPONSAVEL(ID_RESPONSAVEL);
