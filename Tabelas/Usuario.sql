CREATE TABLE USUARIO (
    ID_USUARIO NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NM_USUARIO VARCHAR2(200) NOT NULL,
    EMAIL_USUARIO VARCHAR2(200) NOT NULL,
    SENHA_HASH VARCHAR2(300) NOT NULL,
    SN_ADMINISTRADOR CHAR(1) DEFAULT 'N' NOT NULL,
    SN_ATIVO CHAR(1) DEFAULT 'S' NOT NULL
);

ALTER TABLE USUARIO ADD CONSTRAINT PK_USUARIO_ID PRIMARY KEY (ID_USUARIO);
ALTER TABLE USUARIO ADD CONSTRAINT CK_USUARIO_SN_ADMINISTRADOR CHECK (SN_ADMINISTRADOR IN ('S', 'N'));
ALTER TABLE USUARIO ADD CONSTRAINT CK_USUARIO_SN_ATIVO CHECK (SN_ATIVO IN ('S', 'N'));
