CREATE TABLE CATEGORIA (
    ID_CATEGORIA NUMBER GENERATED BY DEFAULT AS IDENTITY,
    DESCRICAO VARCHAR2(100) NOT NULL,
    ID_MESTRE NUMBER
);

ALTER TABLE CATEGORIA ADD CONSTRAINT PK_CATEGORIA_ID PRIMARY KEY (ID_CATEGORIA);
ALTER TABLE CATEGORIA ADD CONSTRAINT FK_CATEGORIA_ID_MESTRE FOREIGN KEY (ID_MESTRE) REFERENCES CATEGORIA(ID_CATEGORIA);
