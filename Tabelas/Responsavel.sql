CREATE TABLE RESPONSAVEL (
    ID_RESPONSAVEL NUMBER GENERATED BY DEFAULT AS IDENTITY,
    NM_RESPONSAVEL VARCHAR2(200) NOT NULL,
    TP_RESPONSAVEL CHAR(1) NOT NULL,
    DOCUMENTO VARCHAR2(14) NOT NULL,
    NR_TELEFONE VARCHAR2(11),
    EMAIL_RESPONSAVEL VARCHAR2(100),
    SN_ATIVO CHAR(1) DEFAULT 'S' NOT NULL
);

ALTER TABLE RESPONSAVEL ADD CONSTRAINT PK_RESPONSAVEL_ID PRIMARY KEY (ID_RESPONSAVEL);
ALTER TABLE RESPONSAVEL ADD CONSTRAINT CK_RESPONSAVEL_TP CHECK (TP_RESPONSAVEL IN ('F', 'J'));
ALTER TABLE RESPONSAVEL ADD CONSTRAINT CK_RESPONSAVEL_ATIVO CHECK (SN_ATIVO IN ('S', 'N'));
