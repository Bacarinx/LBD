CREATE TABLE SAIDA (
    ID_SAIDA NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_EQUIPAMENTO NUMBER NOT NULL,
    ID_RESPONSAVEL NUMBER NOT NULL,
    ID_USUARIO NUMBER NOT NULL,
    TP_SAIDA VARCHAR2(100) NOT NULL,
    DT_SAIDA DATE NOT NULL,
    OBSERVACAO VARCHAR2(400)
);

ALTER TABLE SAIDA ADD CONSTRAINT PK_SAIDA_ID PRIMARY KEY (ID_SAIDA);
ALTER TABLE SAIDA ADD CONSTRAINT FK_SAIDA_EQUIPAMENTO FOREIGN KEY (ID_EQUIPAMENTO) REFERENCES EQUIPAMENTO(ID_EQUIPAMENTO);
ALTER TABLE SAIDA ADD CONSTRAINT FK_SAIDA_RESPONSAVEL FOREIGN KEY (ID_RESPONSAVEL) REFERENCES RESPONSAVEL(ID_RESPONSAVEL);
ALTER TABLE SAIDA ADD CONSTRAINT FK_SAIDA_USUARIO FOREIGN KEY (ID_USUARIO) REFERENCES USUARIO(ID_USUARIO);
ALTER TABLE SAIDA ADD CONSTRAINT CK_TP_SAIDA CHECK (TP_SAIDA IN ('E', 'O', 'S', 'D', 'C', 'V'));
