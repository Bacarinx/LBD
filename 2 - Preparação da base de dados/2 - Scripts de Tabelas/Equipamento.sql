CREATE TABLE EQUIPAMENTO (
    ID_EQUIPAMENTO NUMBER GENERATED BY DEFAULT AS IDENTITY,
    ID_CATEGORIA NUMBER NOT NULL,
    DESCRICAO VARCHAR2(200) NOT NULL,
    PESO_KG NUMBER,
    CD_BARRAS VARCHAR2(100),
    TP_STATUS CHAR(1) NOT NULL,
    DS_MARCA VARCHAR2(100),
    DS_MODELO VARCHAR2(200)
);

ALTER TABLE EQUIPAMENTO ADD CONSTRAINT PK_EQUIPAMENTO_ID PRIMARY KEY (ID_EQUIPAMENTO);
ALTER TABLE EQUIPAMENTO ADD CONSTRAINT FK_EQUIPAMENTO_CATEGORIA FOREIGN KEY (ID_CATEGORIA) REFERENCES CATEGORIA(ID_CATEGORIA);
